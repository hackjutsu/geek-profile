{
  "name": "projecty",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "bootstrap":"pagic profile",
    "build":"cd profile && pagic build",
    "release":"npm run build; cp -rf profile/public ./docs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "pagic": "^0.5.0"
  }
}
